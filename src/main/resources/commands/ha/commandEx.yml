#----------------------- получаем инфу с устройства
info:
  type: ha
if:
  say:
    variants:
      - температура
      - температура в комнате
      - температура в помещении
do:
  command:
    mode: get #get\send
    entityID: sensor.0xa4c138b8710c2145_temperature



#--------------------------- запрашиваем изменение состояние устройства

info:
  type: ha
if:
  say:
    variants:
      - переключи
      - тумблер
      - тест умного дома
      - тест дома
do:
  command:
    mode: send #get\send
    entityID: input_boolean.test
    requestID: input_boolean/toggle
